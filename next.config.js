/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
    },
    images: {
        domains: ["raghuaiimagegenera79f59b.blob.core.windows.net"],
    },
};

module.exports = nextConfig;
